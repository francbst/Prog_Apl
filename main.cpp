#include <iostream>
#include <string.h>

using namespace std;

int main(void)
{
  string palavra, x, y;
  cout<<"Digitar palavra"<<endl;
  getline(cin, palavra);
  
  for (int i =0; i< palavra.size(); i++)
  {
    cout<<"Digitar letra"<<endl;
    getline(cin, x);
    cout<<"Digitar letra"<<endl;
    getline(cin, y);
    cout<<palavra;
 
  }
  for ()
  return 0;
}
  